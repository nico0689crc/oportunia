name: Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type Check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY || 'pk_test_placeholder' }}
          CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY || 'sk_test_placeholder' }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY || 'sb_publishable_placeholder' }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY || 'sk_placeholder' }}
          GOOGLE_GENERATIVE_AI_API_KEY: ${{ secrets.GOOGLE_GENERATIVE_AI_API_KEY || 'genai_placeholder' }}
          ML_CLIENT_ID: ${{ secrets.ML_CLIENT_ID || 'ml_id_placeholder' }}
          ML_CLIENT_SECRET: ${{ secrets.ML_CLIENT_SECRET || 'ml_secret_placeholder' }}
          ML_REDIRECT_URI: ${{ secrets.ML_REDIRECT_URI || 'http://localhost:3000' }}

  database-migrations:
    name: Supabase Updates
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -q "^supabase/migrations/"; then
            echo "migrations=true" >> $GITHUB_OUTPUT
          else
            echo "migrations=false" >> $GITHUB_OUTPUT
          fi
          if git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -q "^supabase/functions/"; then
            echo "functions=true" >> $GITHUB_OUTPUT
          else
            echo "functions=false" >> $GITHUB_OUTPUT
          fi
        continue-on-error: true

      - name: Setup Supabase CLI
        if: steps.check_changes.outputs.migrations == 'true' || steps.check_changes.outputs.functions == 'true' || github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate SQL syntax
        if: steps.check_changes.outputs.migrations == 'true' || github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
        run: |
          supabase start
          supabase db lint

      - name: Deploy to Staging
        if: github.event_name == 'pull_request'
        run: |
          supabase link --project-ref ${{ secrets.SUPABASE_STAGING_PROJECT_ID }} --password ${{ secrets.SUPABASE_STAGING_DB_PASSWORD }}
          if [ "${{ steps.check_changes.outputs.migrations }}" == "true" ]; then supabase db push; fi
          if [ "${{ steps.check_changes.outputs.functions }}" == "true" ]; then supabase functions deploy; fi
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy to Production
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          supabase link --project-ref ${{ secrets.SUPABASE_PRODUCTION_PROJECT_ID }} --password ${{ secrets.SUPABASE_PRODUCTION_DB_PASSWORD }}
          if [ "${{ steps.check_changes.outputs.migrations }}" == "true" ] || [ "${{ github.event_name }}" == "push" ]; then supabase db push; fi
          if [ "${{ steps.check_changes.outputs.functions }}" == "true" ] || [ "${{ github.event_name }}" == "push" ]; then supabase functions deploy; fi
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
